# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  # fzf commands
  # find all files and multi line select and open in nvim
  - trigger: ":fom"
    replace: nvim $(fd --hidden --exclude .git | fzf -m --preview="bat --color=always {}")
    # find files excluding .git
  - trigger: ":fzf"
    replace: 'fd --hidden --exclude .git | fzf --preview="bat --color=always {}"'
  - trigger: ":sd"
    replace: "sd"

  # docker cli commands
  - trigger: ":dcwatch"
    replace: "watch -n 5 docker ps"
